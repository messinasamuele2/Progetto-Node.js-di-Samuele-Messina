{
  "name": "pof-gas-api",
  "version": "1.0.0",
  "description": "API REST per i gruppi di acquisto solidale di Planty of Food",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "mysql2": "^3.14.0"
  },
  "devDependencies": {
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  }
}
